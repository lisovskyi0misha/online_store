 <% @products.each do |product|%>
    <%= render partial: 'shared/product_card', locals: {
        image: product.image, 
        name: product.name,
        old_price: product.old_price,
        new_price: product.new_price,
        brand: product.brand.name
    }%>
<%end%>